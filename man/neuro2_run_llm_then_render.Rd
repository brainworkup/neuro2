% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neuro2_llm.R
\name{neuro2_run_llm_then_render}
\alias{neuro2_run_llm_then_render}
\title{Run LLM then render Quarto}
\usage{
neuro2_run_llm_then_render(
  base_dir = ".",
  prompts_dir = NULL,
  render_paths = character(0),
  quarto_profile = NULL,
  domain_keywords = c("prnse", "prcog", "pracad", "prverb", "prvis", "prmem", "prexe",
    "prmot", "prsoc", "pradhd_c", "pradhd_a", "premo_c", "premo_a", "pradapt", "prdl",
    "prsirf", "prrec"),
  backend = "ollama",
  mega_for_sirf = FALSE,
  temperature = 0.2,
  echo = "none"
)
}
\arguments{
\item{base_dir}{Base directory where \verb{*_text.qmd} live.}

\item{prompts_dir}{Prompts directory (default installed).}

\item{render_paths}{Character vector of paths to \code{.qmd} files to render after LLM runs.}

\item{quarto_profile}{Optional Quarto profile (e.g., "prod").}

\item{domain_keywords}{Vector of keywords to generate; default is a standard set including "prsirf".}

\item{backend}{\code{"ollama"} (default) or \code{"openai"}.}

\item{mega_for_sirf}{Use the mega model for SIRF.}

\item{temperature}{LLM temperature.}

\item{echo}{ellmer echo mode.}
}
\value{
Invisibly returns a list with \code{llm} results and \code{rendered} output paths.
}
\description{
Executes the LLM stage first, then renders one or more Quarto documents.
}
