% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/neuro2_llm_render_workflow.R
\name{neuro2_run_llm_then_render}
\alias{neuro2_run_llm_then_render}
\title{Run neuro2 LLM stage then render Quarto}
\usage{
neuro2_run_llm_then_render(
  base_dir = ".",
  prompts_dir = NULL,
  domain_keywords = c("priq", "pracad", "prverb", "prspt", "prmem", "prexe", "prmot",
    "prsoc", "pradhdchild", "pradhdadult", "premotchild", "premotadult", "pradapt",
    "prdaily", "prsirf", "prrecs"),
  mega_for_sirf = FALSE,
  model_override = NULL,
  temperature = 0.2,
  render_paths = NULL,
  quarto_profile = NULL,
  quiet = TRUE
)
}
\arguments{
\item{base_dir}{Directory containing the patient/domain *_text.qmd files.
Typically the working directory for a single patient case.}

\item{prompts_dir}{Optional directory with prompt QMDs. During development
pass "inst/prompts". If NULL, uses the installed copy via
\code{system.file("prompts", package = "neuro2")}.}

\item{domain_keywords}{Character vector of domain keywords to run. Defaults to
the standard set including "prsirf" at the end.}

\item{mega_for_sirf}{logical; if TRUE, SIRF uses the 30B / 256k context model.}

\item{model_override}{Optional exact Ollama model name to force for all sections.}

\item{temperature}{Numeric temperature for generation (default 0.2).}

\item{render_paths}{Character vector of report QMD paths to render after LLM stage.
If NULL, no rendering is performed.}

\item{quarto_profile}{Optional Quarto profile name, e.g., "prod".}

\item{quiet}{Render quietly (passed to \code{quarto::quarto_render}).}
}
\value{
A list with fields \code{llm_results} and \code{render_results} (if rendering).
}
\description{
Runs LLM generation for all domains using the prompts in \verb{inst/prompts/}
(or an installed copy), then renders the target Quarto report(s).
}
