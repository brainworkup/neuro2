% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/pluck_neuropsych_test_results.R
\name{pluck_wiat4}
\alias{pluck_wiat4}
\title{Extract and Process WIAT-4 Test Results}
\usage{
pluck_wiat4(
  patient,
  file = NULL,
  pages = c(20),
  extract_columns = c(1, 2, 3, 4, 5),
  score_type = "standard_score",
  variables = c("scale", "raw_score", "score", "ci_95", "percentile"),
  lookup_table_path = "~/Dropbox/neuropsych_lookup_table_combined.csv",
  output_dir = here::here("data", "csv"),
  g_file_name = "2"
)
}
\arguments{
\item{patient}{Character string with the patient's name}

\item{file}{Path to the PDF file. If NULL, a file chooser dialog will be opened.}

\item{pages}{Vector of page numbers to extract from the PDF}

\item{extract_columns}{Vector of column positions to extract}

\item{score_type}{Character string indicating the type of score to use}

\item{variables}{Vector of variable names for the extracted columns}

\item{lookup_table_path}{Path to the lookup table CSV file}

\item{output_dir}{Directory to save output files. Defaults to "data/csv"}

\item{g_file_name}{Name of the consolidated results file, without extension}
}
\value{
A data frame containing the processed WIAT-4 test results
}
\description{
This function extracts data from a WIAT-4 (Wechsler Individual Achievement Test, 4th Edition)
PDF file, processes the extracted data, and generates formatted results with interpretive text.
The function also saves the results to CSV files for further use.
}
\examples{
\dontrun{
# Basic usage with file chooser dialog
results <- pluck_wiat4(patient = "Biggie")

# Specify file path and other parameters
results <- pluck_wiat4(
  patient = "Biggie",
  file = "path/to/wiat4.pdf",
  pages = c(20),
  extract_columns = c(1, 2, 3, 4, 5),
  variables = c("scale", "raw_score", "score", "ci_95", "percentile")
)
}

}
